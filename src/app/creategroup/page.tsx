import Creategroup from '@/components/group/Creategroup';
import { FC } from 'react';

type Props = {};

const page: FC<Props> = ({}) => {
	return (
		<main>
			<Creategroup />
		</main>
	);
};

export default page;
